### Pacifiquem's Profile In Terminal .

This my NPX card unique style to connect with me directly via console or terminal .

### Command
```bash
npx pacifiquem
```

### SCREENSHOT

The final output might look something like this:
<img src="./demo.png"></img>
